Resolved. Cause: DHCP scope for the affected Floor 3 subnet still distributed a decommissioned DNS server after migration, which caused domain controller name resolution failure and Group Policy processing failure at logon. Action: Updated the DHCP scope DNS option to 10.10.0.10, removed stale DNS entries, renewed DHCP leases on affected endpoints, and verified restored DC connectivity and successful Group Policy processing. Preventive: Enforce a migration dependency checkpoint requiring DHCP DNS scope updates to be completed and validated before any DNS decommission is closed. User confirmed working.
